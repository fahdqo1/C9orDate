cd C:\Server

heroku ps:scale web=1 -a gamelibrary

Verify it's running:

heroku ps -a gamelibrary


Run the Flask development server:
flask run

git branch


heroku logs --tail -a gamelibrary


git push heroku main


Make the changes :

git init
git add .
git commit -m "Making Changing in the app.py"

git push heroku master

git push heroku main
